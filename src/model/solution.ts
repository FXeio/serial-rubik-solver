export class Solution {
    web: string;
    arduino: string;
    count: number
    constructor(web: string, arduino: string, count: number) {
        this.web = web;
        this.arduino = arduino;
        this.count = count;
    }
}
